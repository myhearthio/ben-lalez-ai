FROM node:22-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY agents/lead-scoring/ ./agents/lead-scoring/

CMD ["node", "agents/lead-scoring/index.js", "daemon"]
